![Launch poster](https://placehold.co/1280x720/png?text=Launch+Poster)

# Launch Day Update

This fixture places a single remote image before the text body.

It should produce a `media-first` document and a single `send-photo` step with caption because the body stays comfortably below the caption limit.

## Why it exists

- Validates single-image media extraction.
- Confirms caption-based publish planning.
- Provides a compact preview example for `--dry-run`.
